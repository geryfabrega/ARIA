"""Attack pipeline package (Colab runner).

This file exists so `pipeline_colab/attack_pipeline` is importable as
`attack_pipeline` when `pipeline_colab/` is added to `sys.path` in Colab.
"""

from . import config  # re-export for convenience

